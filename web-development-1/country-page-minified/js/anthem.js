const audio=document.getElementById('canada-national-anthem');const play=document.getElementById('cna-play');const pause=document.getElementById('cna-pause');const restart=document.getElementById('cna-restart');const timeline=document.getElementById('audio-timeline');const audioStart=document.getElementById('audio-start');audio.volume=0.2;play.addEventListener('click',()=>{audio.play()});pause.addEventListener('click',()=>audio.pause());restart.addEventListener('click',()=>{audio.currentTime=0;audio.play()});audio.addEventListener('timeupdate',()=>{if(timeline.value===`${Math.round(audio.duration)}`){timeline.value=0;audio.currentTime=0;return audio.pause()}
    audioStart.innerText=formatDuration(audio.currentTime);timeline.value=audio.currentTime});timeline.addEventListener('change',()=>{audio.currentTime=timeline.value});function formatDuration(seconds){const secs=Math.floor(seconds%60);return `${Math.floor(seconds / 60)}:${secs < 10 ? '0' + secs : secs}`}